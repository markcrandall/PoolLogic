.venv/
__pycache__/
*.pyc

# Machine-specific config written by the installer (discovered adapter IP,
# per-site overrides). Kept out of git so `poollogic-update` never has to
# reconcile a locally-modified tracked file.
bridge/config.local.json
